#include <unistd.h>

void	ft_putchar(char c)
{
		write(1, &c, 1);
}

void	ft_print_combn(int n)
{
	int	c;

	c = 0;
	while (++c <= 9) {
		
	}
		ft_putchar(c + '0'); }		
	else {
		
	}
	{
		ft_putchar(nb + '0');
	}
	else
	{
		ft_putnbr(nb / 10);
		ft_putnbr(nb % 10);
	}
}
